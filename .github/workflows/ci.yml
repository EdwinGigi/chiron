name: CI

on:
  push:
    branches: [main]
  pull_request:

jobs:
  lint-and-test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      
      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.12"
          cache: "pip"
          
      - name: Install dependencies
        run: pip install -e '.[dev]'
        
      - name: Lint (Ruff)
        run: |
          ruff check src/ tests/
          ruff format --check src/ tests/
          
      - name: Typecheck (mypy)
        run: mypy src/chiron/
        
      - name: Test (pytest)
        run: pytest tests/ -v --cov=chiron
